#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// ============================================================================
// WIFI CREDENTIALS
// ============================================================================
// VIGTIGT: Kopiér denne fil til "Credentials.h" og indtast dine egne værdier
// Tilføj "Credentials.h" til .gitignore for at beskytte dine passwords!
// ============================================================================

// WiFi netværk indstillinger
#define WIFI_SSID           "DitWiFiNavn"       // Indtast dit WiFi SSID
#define WIFI_PASSWORD       "DitWiFiPassword"   // Indtast dit WiFi password

// WiFi forbindelses indstillinger
#define WIFI_TIMEOUT        20000               // WiFi forbindelses timeout (ms)
#define WIFI_RETRY_DELAY    5000                // Forsøg igen efter (ms)
#define WIFI_MAX_RETRIES    5                   // Maksimalt antal forsøg

// Access Point indstillinger (fallback hvis WiFi fejler)
#define AP_SSID             "RobotMower-AP"     // AP navn
#define AP_PASSWORD         "robotmower123"     // AP password (min. 8 tegn)
#define AP_CHANNEL          1                   // WiFi kanal
#define AP_HIDDEN           false               // Skjul AP
#define AP_MAX_CONNECTIONS  4                   // Maks antal forbindelser

// ============================================================================
// OTA (Over-The-Air) UPDATE CREDENTIALS (fremtidig feature)
// ============================================================================

#define OTA_HOSTNAME        "robot-mower"       // OTA hostname
#define OTA_PASSWORD        "update123"         // OTA password
#define OTA_PORT            8266                // OTA port

// ============================================================================
// MQTT CREDENTIALS (fremtidig feature)
// ============================================================================

#define MQTT_SERVER         "mqtt.example.com"  // MQTT broker adresse
#define MQTT_PORT           1883                // MQTT port
#define MQTT_USER           "robot"             // MQTT brugernavn
#define MQTT_PASSWORD       "password"          // MQTT password
#define MQTT_CLIENT_ID      "robot-mower-001"   // Unikt client ID

#endif // CREDENTIALS_H
